<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>{{ title }}{{ ": " + subtitle if subtitle else ""  }} - {{ site.title }}</title>
        {# NOTE: Twitter uses name='', og uses property='' #}
        <meta content="{{ title }}{{ ": " + subtitle if subtitle else ""  }} - {{ site.title }}" property="title"/>
        <meta content="{{ title }}{{ ": " + subtitle if subtitle else ""  }} - {{ site.title }}" property="og:title"/>
        <meta name="twitter:title" content="{{ title }}{{ ": " + subtitle if subtitle else ""  }} - {{ site.title }}"/>

        {% include "head.njk" %}
    </head>

    <body class="lh-copy pa0 f5 f4-ns sans-serif">
        <script>
            {% include "dark-mode.js" %}
        </script>

        {% block body %}{% endblock %}
    </body>

</html>
